#include <string>
#include <iostream>
#include <map>
#include <vector>
#include <queue>
using namespace std;

map<string, bool> check;
int main()
{
	vector<vector<int> > order;
	
	order.push_back({ { 2,3 }, {3,4} });
	int ordersize = order.size();
	queue<int> q;
	priority_queue<int> pq;
	
	for (register int i = 0; i < ordersize; i++) {
		
	}
}