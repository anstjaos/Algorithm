```java
class Solution {
    public int missingMultiple(int[] nums, int k) {
        boolean[] isExists = new boolean[100005];

        for (int num : nums) {
            isExists[num] = true;
        }

        int result = k;
        while (isExists[result]) {
            result += k;
        }

        return result;
    }
}
```