```java
class Solution {
    public int minOperations(int[] nums) {
        int count = 0;
        for (int num : nums) {
            if (num == nums[0]) count++;
        }

        return count < nums.length ? 1 : 0;
    }
}
```