```java
class Solution {
    public int elevatorRequests(int n, int[] requests) {
        int cur = 0, result = 0;
        for (int request : requests) {
            result += Math.abs(request - cur);
            cur = request;
        }

        return result;
    }
}
```