```java
class Solution {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> isVisit = new HashSet<>();
        for (int num : nums) {
            if (isVisit.contains(num)) {
                return true;
            }

            isVisit.add(num);
        }

        return false;
    }
}
```